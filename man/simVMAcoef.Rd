% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simVMA.R
\name{simVMAcoef}
\alias{simVMAcoef}
\title{Generate VMA Coefficients}
\usage{
simVMAcoef(
  p,
  order,
  nonzero_ratio = 1/60,
  min = -0.4,
  max = 0.6,
  same_lag_matrix = FALSE
)
}
\arguments{
\item{p}{A positive integer dimension.}

\item{order}{A positive integer VMA order.}

\item{nonzero_ratio}{Proportion of non-zero entries. Must be in \verb{(0, 1]}.}

\item{min}{Lower bound for non-zero coefficients.}

\item{max}{Upper bound for non-zero coefficients.}

\item{same_lag_matrix}{Logical. If \code{TRUE}, use the same matrix for all lags.}
}
\value{
A numeric array with dimension \code{c(p, p, order)}.
}
\description{
Generates sparse coefficient matrices for a vector moving-average model.
}
\details{
The returned array stores the coefficient matrices in
\deqn{x_t = z_t + \sum_{h=1}^q A_h z_{t-h}.}
The slice \code{coef[, , h]} corresponds to \code{A_h}.
}
\examples{
set.seed(123)
coef <- simVMAcoef(p = 5, order = 2)
dim(coef)
}
