% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covVMA.R
\name{covVMA}
\alias{covVMA}
\title{Estimate Lagged Cross-Covariance Matrix}
\usage{
covVMA(x, lag)
}
\arguments{
\item{x}{Numeric matrix or data frame. Rows are time points and columns are variables.}

\item{lag}{Non-negative integer lag.}
}
\value{
A \code{p} by \code{p} matrix containing the estimated lagged cross-covariances.
}
\description{
Computes the lagged cross-covariance matrix for a multivariate time series.
}
\details{
For an \code{n} by \code{p} observation matrix \eqn{X}, this function computes
\deqn{\widehat{\Gamma}(k) =
      \frac{1}{n}\sum_{t = 1}^{n-k} X_t X_{t+k}^{\top},}
where \eqn{k} is the requested lag.
}
\examples{
x <- matrix(rnorm(100 * 4), nrow = 100, ncol = 4)
covVMA(x, lag = 1)
}
