% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simVMA.R
\name{simVMA}
\alias{simVMA}
\title{Simulate a Vector Moving-Average Process}
\usage{
simVMA(
  n,
  p,
  order,
  coeff = NULL,
  nonzero_ratio = 1/60,
  min = -0.4,
  max = 0.6,
  same_lag_matrix = FALSE,
  innov = NULL,
  innov_cov = diag(p),
  burnin = 0,
  seed = NULL
)
}
\arguments{
\item{n}{A positive integer sample size.}

\item{p}{A positive integer dimension.}

\item{order}{A positive integer VMA order.}

\item{coeff}{An optional coefficient array with dimension \code{c(p, p, order)}.}

\item{nonzero_ratio}{Proportion of non-zero entries used when \code{coeff = NULL}.}

\item{min}{Lower bound for generated coefficients.}

\item{max}{Upper bound for generated coefficients.}

\item{same_lag_matrix}{Logical. If \code{TRUE}, use the same coefficient matrix
for all lags.}

\item{innov}{An optional innovation matrix.}

\item{innov_cov}{Covariance matrix for generated Gaussian innovations.}

\item{burnin}{Number of initial observations to discard.}

\item{seed}{An optional random seed.}
}
\value{
A list containing the simulated data, coefficients, innovations,
order, dimension, and sample size.
}
\description{
Simulates a multivariate VMA process with optional user-specified coefficients.
}
\details{
The simulated process is
\deqn{
x_t = z_t + \sum_{h=1}^q A_h z_{t-h},
}
where \code{z_t} is white noise and \code{A_h} is the coefficient matrix at lag \code{h}.
}
\examples{
set.seed(123)
fit <- simVMA(n = 100, p = 5, order = 2)
head(fit$x)
}
